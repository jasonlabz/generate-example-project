# Health Check Controller 模块化：提交记录

> **历史提交草案。** 本文列出的文件和 commit 切分早于 Wire、模块内 Service/Manager 契约及路径化 Mock 改造，不能作为当前提交清单。用户已明确要求本次不提交；若未来授权提交，必须从当前 `git status` 与 [实施计划](../../superpowers/plans/2026-08-15-huma-module-layout-and-mockgen.md) 重新生成范围。

## 提交上下文

- 入口：主线 release 通过后的提交决策。
- 计划：[`07-代码规划.md`](./07-代码规划.md)；实现、评审和验证报告均已通过。
- 当前交付单元：`TASK-01`；需求类型；`task_card_id: n-a`（M 级、用户未要求 iCafe Task）；无 pending Task。
- 分支：`master`；HEAD：`d5c2c88f2f628e4e2623e5433539648c3b749516`。
- remote：`git@github.com:jasonlabz/aiib-go.git`；非 iCode，因此只提供本地提交，不执行 push/CR。
- 验证：`10-验证报告.md` 的 release 已通过；范围在验证后未变化。

## 问询前生成方案

建议拆成两个本地 commit，以保持功能改造与独立历史 lint 债务可单独回滚、cherry-pick：

| commit message | 精确文件 | 原子性理由 |
| --- | --- | --- |
| `refactor(health-check): modularize controller with injectable service` | `server/controller/health_check.go`、`server/controller/health_check_test.go`、`server/controller/health_check/**`、`server/router/router.go`、`server/service/health_check.go`、`server/service/health_check/health_check_impl.go`、`server/service/health_check/health_check_impl_test.go`、`server/mocks/generate.go`、`server/mocks/mock_controller.go`、`server/mocks/mock_service.go` | Health Check 模块、分层接口、Router 与生成 Mock 共享同一内部契约。 |
| `fix(ginx): resolve existing Go lint findings` | `bootstrap/migrate.go`、`common/ginx/page.go`、`common/ginx/response.go` | 与 Health Check 契约独立；只修复 7 项已复现 lint finding。 |

- `docs/vibe-coding/**` 是本次过程记录，当前保持未跟踪；不包含在默认提交范围，除非用户另行要求。

## 结构化提交决策

- 当前运行模式没有可用的原生单选组件；等待用户明确选择。
- 可选动作：创建上述两个本地 commit / 合并为一个本地 commit / 暂不提交。
- 尚未执行 Git 写入、push 或 CR。

## 执行结果

- 状态：`blocked`（等待用户一次性提交授权）。
- 下一步：收到用户选择后，在写入前重新核对精确文件范围和 HEAD；范围变化则重新生成方案。
