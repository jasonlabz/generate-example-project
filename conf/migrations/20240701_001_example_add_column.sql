-- ============================================================
-- 增量迁移示例（YYYYMMDD_NNN_desc.sql）
-- ============================================================
-- 约定：
--   1. 文件名为 日期_NNN_描述.sql，如 20240701_001_add_email.sql
--   2. 系统按文件名排序依次执行未应用的迁移
--   3. 每个文件包含本次变更的 DDL（ALTER TABLE / CREATE TABLE 等）
--   4. 已在 schema_migrations 中记录的版本不会重复执行
-- ============================================================

-- 示例：给 users 表增加 email 字段
-- ALTER TABLE users ADD COLUMN IF NOT EXISTS email VARCHAR(255);
