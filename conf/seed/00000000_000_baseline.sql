-- @version 20260903_001
-- seed 基线：覆盖到 20260903_001，后续仅执行版本更高的 seed 文件。

-- ============================================================
-- 种子数据示例
-- ============================================================
-- 约定：
--   1. 所有 INSERT 必须使用 ON CONFLICT ... DO NOTHING/DO UPDATE 保证幂等
--   2. 首次部署或有新版本时，在全部 DDL 迁移完成后执行
--   3. 执行失败不会阻塞启动（仅告警日志）
--   4. 按版本号、文件名顺序依次执行
-- ============================================================

-- 示例：默认角色
-- INSERT INTO roles (code, name, description) VALUES
--     ('R_SUPER', '超级管理员', '拥有所有权限'),
--     ('R_ADMIN', '管理员', '拥有管理权限'),
--     ('R_USER', '普通用户', '基本操作权限')
-- ON CONFLICT (code) DO NOTHING;
