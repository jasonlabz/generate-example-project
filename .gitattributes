# 统一换行符：仓库内一律 LF，检出时不按平台转换。
#
# 项目只面向 Linux 部署与类 Unix 构建环境（Linux / macOS / WSL / Git Bash），
# 没有需要保留 CRLF 的 Windows 脚本（.ps1 / .bat / .cmd）。
# 固定 LF 可以避免 gofmt -l 因 CRLF 误报，也让跨平台 diff 保持干净。
* text=auto eol=lf
